{
  "name": "compass-crew",
  "version": "2.1.0",
  "description": "CompassCrew — auditable agent-fleet control inside Obsidian: recursive canvas, bearings navigation, a direct reckon MCP client, and in-client chain-of-custody verification. Non-extractable signing keys. Zero runtime dependencies.",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "node esbuild.config.mjs production",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "smoke": "node scripts/smoke/coc_contract_smoke.mjs",
    "verify": "npm run typecheck && npm run build && npm run test && npm run smoke"
  },
  "keywords": [
    "obsidian",
    "agents",
    "mcp",
    "chain-of-custody",
    "excalidraw",
    "provenance",
    "compass-crew",
    "ed25519"
  ],
  "author": "goodoleusa",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "builtin-modules": "^3.3.0",
    "esbuild": "^0.20.0",
    "fake-indexeddb": "^6.0.0",
    "obsidian": "latest",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
