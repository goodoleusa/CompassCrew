{
  "name": "compass-crew",
  "version": "2.1.0",
  "description": "CompassCrew \u2014 mission control for AI agent crews inside Obsidian: recursive canvas, bearings-based navigation, and an MCP bridge for steering agents from your vault. Zero runtime dependencies.",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "node esbuild.config.mjs production",
    "typecheck": "tsc --noEmit",
    "smoke": "node scripts/smoke/coc_contract_smoke.mjs",
    "verify": "npm run typecheck && npm run build && npm run smoke"
  },
  "keywords": [
    "obsidian",
    "agents",
    "compass-crew",
    "blueprint",
    "mcp",
    "compass",
    "excalibrain"
  ],
  "author": "goodoleusa",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "builtin-modules": "^3.3.0",
    "esbuild": "^0.20.0",
    "obsidian": "latest",
    "typescript": "^5.4.0"
  }
}
